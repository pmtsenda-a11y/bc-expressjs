# ============================================================
# PASO 1: Trigger y checkout
# ============================================================
# Descomenta el bloque del PASO 1.
# Objetivo: definir el nombre, triggers y el primer step.
#
# Referencia: 1-teoria/02-github-actions.md
# Verificación: GitHub → pestaña Actions → debe aparecer "CI"
# ============================================================

# name: CI
#
# on:
#   push:
#     branches: [main]
#   pull_request:
#     branches: [main]
#
# jobs:
#   test:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4

# ============================================================
# PASO 2: Setup pnpm + Node.js + cache
# ============================================================
# Dentro del job test, agrega estos steps después de checkout.
# El cache evita reinstalar deps en cada run.
# ============================================================

#       - name: Setup pnpm
#         uses: pnpm/action-setup@v4
#         with:
#           version: 10
#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '22'
#           cache: 'pnpm'
#       - name: Install dependencies
#         run: pnpm install --frozen-lockfile

# ============================================================
# PASO 3: Build + Tests
# ============================================================
# Agrega los dos steps finales del pipeline.
# ============================================================

#       - name: Build
#         run: pnpm build
#       - name: Test
#         run: pnpm test

# ============================================================
# PASO 4: Variables de entorno
# ============================================================
# Agrega el bloque env: al job (no a un step individual).
# Reemplaza el valor con el secret de GitHub.
# ============================================================

#     env:
#       NODE_ENV: test
