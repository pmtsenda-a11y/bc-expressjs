# ============================================================
# CI/CD Pipeline
# ============================================================
# TODO 1: Completar el workflow CI con los steps correctos
# ============================================================

# name: CI
#
# on:
#   push:
#     branches: [main]
#   pull_request:
#     branches: [main]
#
# jobs:
#   test:
#     runs-on: ubuntu-latest
#     env:
#       NODE_ENV: test
#       DATABASE_URL: postgresql://pguser:pgpassword@localhost:5432/testdb
#     steps:
#       # TODO: Agrega los steps de checkout, pnpm, node y test
#       #
#       # PISTA — Usa estos actions en este orden:
#       # 1. actions/checkout@v4
#       # 2. pnpm/action-setup@v4  (version: 10)
#       # 3. actions/setup-node@v4  (node-version: '22', cache: 'pnpm')
#       # 4. run: pnpm install --frozen-lockfile
#       # 5. run: pnpm build
#       # 6. run: pnpm test
