# ============================================================
# TODO 8: Completar el pipeline CI/CD
# ============================================================
# Agrega los steps necesarios al job 'test'.
# Referencia: week-15/2-practicas/ejercicio-01-github-actions/
# ============================================================

# name: CI
#
# on:
#   push:
#     branches: [main]
#   pull_request:
#     branches: [main]
#
# jobs:
#   test:
#     runs-on: ubuntu-latest
#     env:
#       NODE_ENV: test
#       DATABASE_URL: postgresql://pguser:pgpassword@localhost:5432/testdb
#       JWT_SECRET: ${{ secrets.JWT_SECRET }}
#       JWT_REFRESH_SECRET: ${{ secrets.JWT_REFRESH_SECRET }}
#
#     steps:
#       # TODO: Agrega los steps en este orden:
#       # 1. actions/checkout@v4
#       # 2. pnpm/action-setup@v4   (version: 10)
#       # 3. actions/setup-node@v4  (node-version: '22', cache: 'pnpm')
#       # 4. run: pnpm install --frozen-lockfile
#       # 5. run: pnpm build
#       # 6. run: pnpm test
