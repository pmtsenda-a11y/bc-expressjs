# ============================================================
# TODO 3: Completar docker-compose.yml
#
# Servicios requeridos:
# - app: construido desde Dockerfile, puerto 3000, env_file, depends_on postgres
# - postgres: imagen postgres:16-alpine, healthcheck pg_isready, volume pg_data
#
# Red: app_network (interna)
# Volumes: pg_data (named, para persistencia)
#
# Referencia: 1-teoria/04-docker-compose.md
# ============================================================

services:

  # TODO: Completar el servicio app
  # app:
  #   build: .
  #   ports:
  #     - "3000:3000"
  #   env_file:
  #     - .env.docker
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   command: sh -c "pnpm prisma migrate deploy && node dist/server.js"
  #   networks:
  #     - app_network
  #   restart: unless-stopped

  # TODO: Completar el servicio postgres
  # postgres:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_USER: pguser
  #     POSTGRES_PASSWORD: pgpassword
  #     POSTGRES_DB: myapp
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U pguser -d myapp"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5
  #     start_period: 10s
  #   volumes:
  #     - pg_data:/var/lib/postgresql/data
  #   networks:
  #     - app_network
  #   restart: unless-stopped

# TODO: Declarar la red
# networks:
#   app_network:

# TODO: Declarar el volume
# volumes:
#   pg_data:
